databaseChangeLog:
  - logicalFilePath: no-path
  - changeSet:
      id: init change set
      author: Alisher
      preConditions:
        onFail: MARK_RAN
        not:
          tableExists:
            schemaName:
            tableName: booking
      changes:
        - createTable:
            tableName: test_table
            columns:
              - column:
                  name: id
                  type: int
                  autoIncrement: true
                  remarks: 'Test table id pk'
                  constraints:
                    primaryKey: true
                    primaryKeyName: test_id_pk
              - column:
                  name: name
                  type: varchar(100)
                  remarks: 'name column'
                  constraints:
                    nullable: false